name: langgraph-quickstart
description: Quick evaluation of LangGraph agent
agent: langgraph-example
pack: quickstart
faults:
  - llm_rate_limit
  - tool_timeout
security:
  enabled: true
  threshold: 80
